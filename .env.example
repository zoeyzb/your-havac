# Demo Mode
# Set to 'true' to run without a Drupal backend using sample data.
# To REMOVE demo mode from a production project, see lib/demo-mode.ts for instructions.
# NEXT_PUBLIC_DEMO_MODE=true

# Drupal Configuration
# Your Drupal site URL (e.g., https://mysite.decoupled.io or http://mysite.localhost:8888 for local)
NEXT_PUBLIC_DRUPAL_BASE_URL=https://your-site.decoupled.website

# OAuth2 Client Credentials
# Found in Drupal admin at: /admin/config/decoupled/oauth
DRUPAL_CLIENT_ID=your-client-id-here
DRUPAL_CLIENT_SECRET=your-client-secret-here

# Revalidation Secret
# Used to secure the revalidation endpoint
# Generate a random string: openssl rand -hex 32
DRUPAL_REVALIDATE_SECRET=your-revalidate-secret-here

# Site Configuration (Optional)
# Explicit site URL for metadata and canonical URLs
# NEXT_PUBLIC_SITE_URL=https://www.example.com

# Server Configuration (Optional - for local development)
# PORT=3000
# HOST=localhost

# Environment
# NODE_ENV=development

# Claude Code MCP Integration
# Get your API token from: https://dashboard.decoupled.io/settings/api-tokens
DECOUPLED_API_TOKEN=dc_tok_your_token_here
